
events {
  worker_connections  1024;
}

http {
  server { # simple load balancing
    listen          80;
    location / {  
      index      index.html;
      root /usr/share/nginx/html/;
    }
  }
}